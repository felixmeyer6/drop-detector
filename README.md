# drop-detector
Finds the main drop (high-energy part) of an EDM track. Uses lightweight audio segmentation and a linear SVM classifier to score candidate boundaries and return the top-2 most likely drop moments.
